# Transcription (tourne en local, sur CPU)
# faster-whisper 1.0.x exige `av<13`, une plage sans wheel pour Python 3.13+ ;
# 1.1.0+ leve ce plafond (av>=11 seulement).
faster-whisper>=1.1.0
requests==2.34.2
# numpy : version plancher (sans plafond) plutot qu'un pin exact. Paquet
# compile dont le wheel disponible depend de la version de Python installee ;
# un pin exact trop ancien echoue a l'installation sur une version de Python
# recente (pas de wheel -> pip tente une compilation, qui echoue sans
# compilateur C installe).
numpy>=1.26.4

# Serveur web
fastapi>=0.115
uvicorn>=0.30
